package algorithm.sort;

public class SelectionSort implements Sort{
    /**
     *  选择排序（Selection Sort）
     *  选择排序是一种简单的排序算法，
     *  它的基本思想是每次从待排序的元素中选出最小（或最大）的一个元素，存放在序列的起始位置，直到全部待排序的元素排完。
     * */
    public void selectionSort(){

    }

    @Override
    public void sort(int[] nums) {

    }

    @Override
    public int order() {
        return 0;
    }
}
