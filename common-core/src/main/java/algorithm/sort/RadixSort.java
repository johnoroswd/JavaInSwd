package algorithm.sort;

public class RadixSort implements Sort{
    /**
     * 基数排序（Radix Sort）
     * 基数排序是一种非比较排序算法，它的基本思想是将待排序的数组按照位数（个位、十位、百位）进行划分，
     * 然后依次对每个位上的数字进行排序，最终得到有序的数组。
     * */
    public void radixSort(){

    }

    @Override
    public void sort(int[] nums) {

    }

    @Override
    public int order() {
        return 0;
    }
}
